package proj.elevator.system.model.param;

import proj.elevator.system.model.Direction;

/**
 * wrapper object for json conversion purposes.
 * @param direction
 */
public record DirectionParam(Direction direction) {
}
